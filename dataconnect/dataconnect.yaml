specVersion: "v1beta"
serviceId: "archival_system"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "archival_system-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
